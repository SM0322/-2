<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8"/>
    <title>Bookers</title>
  </head>

<body>
<h1 class="top-title">Editing Book</h1>

<% if @book.errors.any? %>
  <%= @book.errors.count %> errors prohibited this book from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<%= form_with model: @book, url: book_path(@book.id), method: :patch do |f| %>

  <div class="new-box">
    <h4>Title<h4>
  <%= f.text_field :title %>
  </div>

  <div class="new-box">
  <h4>Body</h4>
  <%= f.text_area :body %>
  </div>

  <div class="new-box">
  <%= f.submit 'Update Book' %>
  </div>
  <div class="show-box1">
  <%= link_to "Show", book_path(@book.id) %>
  |
  <%= link_to "Back",'/books' %>
  </div>

<% end %>
</body>